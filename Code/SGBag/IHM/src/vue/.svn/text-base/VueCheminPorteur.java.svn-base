package vue;

import bibliothequesTiers.Point;

import java.awt.Graphics;

import noyau.CheminPorteur;

/**Classe gérant l'affichage des chemins porteurs.
 */
public class VueCheminPorteur extends VueChemin {

     /**Constructeur de la classe VueCheminPorteur
     * @param cheminPorteur Objet CheminPorteur géré.
      * @param vueAeroport L'objet vue de l'aéroport.
     */
    public VueCheminPorteur(CheminPorteur cheminPorteur, VueAeroport vueAeroport){
        super(cheminPorteur, cheminPorteur.getTheta(), vueAeroport);
    }
}
